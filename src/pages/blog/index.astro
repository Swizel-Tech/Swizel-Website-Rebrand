---
import { getCollection } from 'astro:content';
import MainLayout from '../../layouts/MainLayout.astro';
import Breadcrumb from '../../components/blocks/Breadcrumb.astro';
import BlogPostCard from '../../components/elements/BlogPostCard.astro';
import Section from '../../components/HTML/Section.astro';
import FooterCTA from '../../components/blocks/FooterCTA.astro';
import NewsLetterAndContact from '../../components/sections/NewsLetterAndContact.astro';
import BlogBoardroom from '../../components/sections/BlogBoardroom.astro';
import BlogFounder from '../../components/sections/BlogFounder.astro';
import BlogBuilder from '../../components/sections/BlogBuilder.astro';
import BlogStudio from '../../components/sections/BlogStudio.astro';
import BlogCampus from '../../components/sections/BlogCampus.astro';

// Published posts, newest first. Drafts (draft: true) are kept out of the build.
export const posts = (await getCollection('blog', ({ data }) => data.draft !== true)).sort(
	(a, b) => b.data.pubDate.valueOf() - a.data.pubDate.valueOf()
);
---

<MainLayout
	title="The Swizel Journal | Insights on Software, Design & Marketing"
	image="/images/og/og-blog.jpg"
	imageSquare="/images/og/og-blog-sq.jpg"
	description="Ideas, build notes and insights from the Swizel team on software, design, AI and digital marketing for businesses across Africa and beyond."
>
	<!-- Boardroom world (also the default). Hidden in the other views. -->
	<BlogBoardroom posts={posts} />

	<!-- Founder world: the same journal, founder momentum. -->
	<BlogFounder posts={posts} />

	<!-- Builder world: the journal/ directory. -->
	<BlogBuilder posts={posts} />

	<!-- Studio world: the Reading Room of the gallery. -->
	<BlogStudio posts={posts} />

	<!-- Campus world: the school paper. -->
	<BlogCampus posts={posts} />

	<!-- Fallback for the remaining worlds. -->
	<div class="alt-view-only blog-fallback">
		<Breadcrumb title="Articles" desc="" />
		<Section divClass="py-20">
			<ul
				class="grid grid-cols-1 sm:grid-cols-2 lg:grid-cols-3 gap-10 pb-20 border-b border-swizelBrown"
			>
				{
					posts.map((post) => (
						<BlogPostCard as="li" to={`/blog/${post.slug}`} {...post} />
					))
				}
			</ul>
		</Section>
		<NewsLetterAndContact hideContact />
		<FooterCTA />
	</div>
</MainLayout>

<style is:global>
	/* Founder, Builder and Studio views have their own Blog now — hide the
	   generic fallback there. */
	html[data-view='founder'] .blog-fallback,
	html[data-view='builder'] .blog-fallback,
	html[data-view='studio'] .blog-fallback,
	html[data-view='campus'] .blog-fallback {
		display: none !important;
	}
</style>

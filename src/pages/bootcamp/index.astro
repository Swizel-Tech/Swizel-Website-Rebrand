---
// The open bootcamp is retired. Everything now lives on /programs, so
// old links, old shares and old search results land there instead of a
// page that promises something we no longer run.
return Astro.redirect('/programs', 301);
---
